<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="8dp">

    <TextView
        android:id="@+id/adapter_ingredient_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textStyle="bold"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Title" />

    <TextView
        android:id="@+id/adapter_ingredient_description"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        app:layout_constraintStart_toStartOf="@+id/adapter_ingredient_title"
        app:layout_constraintTop_toBottomOf="@+id/adapter_ingredient_title"
        tools:text="Description" />

    <TextView
        android:id="@+id/adapter_ingredient_amount"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="8dp"
        app:layout_constraintBottom_toBottomOf="@+id/adapter_ingredient_units"
        app:layout_constraintEnd_toStartOf="@+id/adapter_ingredient_units"
        app:layout_constraintTop_toTopOf="@+id/adapter_ingredient_units"
        tools:text="500" />

    <TextView
        android:id="@+id/adapter_ingredient_units"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="@+id/adapter_ingredient_description"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@+id/adapter_ingredient_title"
        tools:text="Gram" />
</androidx.constraintlayout.widget.ConstraintLayout>